{
  "name": "upload",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lambda:build": "esbuild src/lambda.ts --bundle --platform=node --target=node22 --outfile=dist/lambda.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.717.0",
    "@aws-sdk/s3-request-presigner": "^3.717.0",
    "sst": "^3.19.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "typescript": "5.9.2"
  }
}
