{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lambda:build": "esbuild src/lambda.ts --bundle --platform=node --target=node20 --outfile=dist/lambda.js"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/database": "*",
    "@repo/identity": "*",
    "@trpc/server": "^11.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.2",
    "typescript": "5.9.2"
  }
}
