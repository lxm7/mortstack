{
  "name": "@repo/chat-mls-core",
  "version": "0.0.1",
  "private": true,
  "description": "OpenMLS engine for Sessions chat — Rust core + UniFFI bindings (see ADR-015)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build:android": "./scripts/build-mls.sh android",
    "build:ios": "./scripts/build-mls.sh ios",
    "lint": "echo \"rust crate + autogen bindings — skip eslint\" && exit 0",
    "check-types": "tsc --noEmit",
    "test": "echo \"smoke is the build itself — see scripts/README.md\" && exit 0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/react": "~19.1.10",
    "typescript": "5.9.2"
  },
  "peerDependencies": {
    "expo": "*",
    "react": "*",
    "react-native": "*"
  }
}
