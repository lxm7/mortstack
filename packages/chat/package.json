{
  "name": "@repo/chat",
  "version": "0.0.0",
  "private": true,
  "description": "Chat UI components, hooks, screens and store for Sessions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "check-types": "tsc --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@repo/chat-crypto": "workspace:*",
    "@repo/chat-db": "workspace:*",
    "@repo/chat-transport": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/react": "~19.1.10",
    "eslint": "^9.39.1",
    "typescript": "5.9.2"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  }
}
